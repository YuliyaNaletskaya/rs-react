import MainPage from '../pages/MainPage';
import type { RoutePages } from '../types/routePages';

export const routes: RoutePages[] = [
  { path: '/', element: MainPage },
  // { path: '/about', element: AboutPage },
];
